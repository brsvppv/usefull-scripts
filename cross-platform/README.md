# Cross-platform scripts

Place scripts that run on multiple OSes here. Prefer:
- Python (with a tiny `cli.py` wrapper and `requirements.txt`)
- Node.js (with `package.json` and a small CLI)

Each utility should include a short README explaining supported OSes and how to run it.
