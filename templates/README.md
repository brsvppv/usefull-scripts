# Templates

This folder contains templates to help contributors add new scripts consistently.

Suggested templates:
- `script.sh.template` - header, usage, exit codes
- `script.ps1.template` - param block, help comment, examples
- `python-cli.template` - tiny `argparse` scaffolding
