# macOS scripts

Use `common/` for macOS helpers. Prefer POSIX-compliant shell scripts or small AppleScript/osascript wrappers when necessary.

Document macOS-specific requirements (Homebrew, system extensions) in the folder README.
